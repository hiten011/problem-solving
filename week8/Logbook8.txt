Problem: CorporationSalary

Design Thinking:
The goal was to calculate the total salary of all employees in a company where each employee’s salary depends on the salaries of the employees they manage.
If an employee manages no one, their salary is 1. Otherwise, their salary is the sum of the salaries of all employees they manage.

This forms a directed acyclic graph (DAG), where each employee is a node and edges point to subordinates. The problem is naturally solved using DFS + memoization, since we can recursively compute salaries and store results to avoid recomputation.

Troubleshooting:

At first, I was unsure how to handle employees with no subordinates. Adding the base case return salaries[curNode] = (ans ? ans : 1) fixed that neatly.

I had to ensure I didn’t recompute the same salary multiple times, so I used a salaries vector initialized with -1 and memoized each result.

Another small fix was skipping over already-computed values when summing the final answer, ensuring no duplicates or uninitialized states were included.

Reflection:
This problem taught me how DFS with memoization can turn what looks like an exponential recursion into an efficient solution. I also understood the importance of identifying dependencies between nodes (like manager → subordinate) and computing them in a bottom-up manner through recursion.

The clean recursive structure made it easy to implement once I set up proper memoization and base conditions.

Complexity:

Time: O(N²) since each relation can be checked once.

Space: O(N) for recursion and memoization.



Problem: FillBox

Design Thinking:
The task was to fill a box of given dimensions using cubes of different sizes (powers of 2), minimizing the total number of cubes used. Each cube size has a limited count.

I initially tried to solve this recursively — breaking down the box into smaller parts after placing one cube — but it didn’t work properly due to incorrect space division and overlap handling. Then I tried a volume-based approach (filling volume proportionally to cube size), but that also failed because it didn’t account for spatial fitting (3D geometry constraints).

Troubleshooting:

The recursive approach failed because I didn’t handle all sub-box combinations correctly after placing a cube.

Using pow(2, i) as a map key caused precision issues since it’s a floating-point function — integer powers would be safer.

I realized I also wasn’t tracking how smaller cubes fill leftover space after larger cubes are placed.

After several test failures and incorrect counts, I decided to pause this problem and move on to CorporationSalary to refresh my recursion skills.

Reflection:
This problem showed me that 3D recursive decomposition requires careful spatial reasoning and a systematic breakdown of remaining box space. My recursion idea was logically correct but incomplete in handling all possible sub-boxes.

I plan to revisit this problem later — probably using a greedy + volume fitting strategy (starting from largest cubes and filling downwards) rather than direct recursion.

For now, I moved to CorporationSalary, which helped me rebuild confidence in recursive and memoization-based thinking.


Problem: [ProblemsToSolve]

Design Thinking:
The goal was to determine the minimum number of problems you need to solve from a sequence so that the difference between the maximum and minimum “pleasantness” reaches or exceeds a given threshold (variety).

Initially, I approached this problem naively, tracking just a single minIndex and maxIndex for the entire sequence. The first versions attempted to update minIndex and maxIndex as I iterated, comparing the current element with previous extremes and applying some formula to calculate the number of problems solved.

As I encountered parity constraints (since you can only solve problems at every other index in some cases), I realized I needed separate tracking for odd- and even-indexed problems.

Troubleshooting:

Early versions: Only tracked one minIndex and one maxIndex. This produced incorrect results when the minimum and maximum pleasantness values occurred at indices of different parity, failing some test cases.

Handling ties: At first, I didn’t properly handle cases where pleasantness values were equal but the index parity differed. I added explicit rules like i % 2 == 0 to prioritize certain indices.

Updating min/max separately by parity: Eventually, I realized I needed four trackers:

evenMinIndex, evenMaxIndex for even indices

oddMinIndex, oddMaxIndex for odd indices
This allowed me to calculate maximum differences within the same parity and across parity separately, which was key to computing the minimum number of problems correctly.

Calculating answer: I initially used (i / 2) + 1 or (i / 2) + 2 inconsistently. Careful tracing of examples helped me identify exactly how many problems are needed depending on parity and which index triggered the difference.

Reflection:
This problem highlighted the importance of tracking multiple parallel states (here, odd/even indices) when simple global min/max is insufficient. Early versions failed because I ignored parity effects on problem selection, and careful debugging showed that subtle off-by-one or parity errors can completely break correctness.

I also learned the value of iteratively refining your solution: starting with a simple idea, adding special handling for ties, then separating odd/even tracking, and finally producing a clean and fully working version. The process reinforced the importance of carefully reading problem constraints and walking through small examples to validate edge cases.

Complexity:

Single pass through the array: O(n)

Constant-time updates for four trackers per iteration: O(1)

Total: O(n)


Problem: [RockStar]

Design Thinking:
The task was to compute the maximum number of songs the rock star can perform given counts of four song types (ff, fs, sf, ss) while respecting tempo alternation rules.

The approach was greedy:

Start by performing all ff songs.

Use fs songs to transition between fast and slow tempos.

Pair sf songs with remaining fs to maintain alternation.

Add ss songs at the end since slow-slow songs can follow any slow song.

Handle any leftover unpaired sf song for an extra performance.

Troubleshooting:

Initial attempts: Simply summing song counts failed, ignoring tempo alternation.

Second attempt: Greedy pairing worked for many cases but failed when sf > fs (extra slow-fast songs left unpaired).

Final fix: After pairing, if there are more sf than fs, one additional song can still be performed. This adjustment correctly handles edge cases.

Reflection:
This problem highlighted the importance of handling edge cases in greedy algorithms. While the main greedy strategy (pairing songs to respect alternation) solves most scenarios, the leftover unpaired songs require careful consideration.

I also learned the value of small test simulations to identify patterns where the naive greedy approach might fail, guiding the final adjustment (if sf > fs return ans + 1).

Complexity:

Time: O(1) — constant operations.

Space: O(1) — only a few variables used.



Problem: [HandsShaking]

Design Thinking:
The goal of this problem was to calculate the number of ways n people can shake hands such that no two handshakes cross. This is a classic Catalan number problem, where each handshake divides the circle into two smaller, independent handshake subproblems.

I used dynamic programming (DP) to compute the solution efficiently.

Let dp[i] represent the number of valid handshake arrangements for i people.

The recurrence relation is based on pairing the first person with another, splitting the remaining into two groups:
    Base case: dp[0] = 1 (no people, one valid configuration).

    Only even n are valid (odd n means one person is left unpaired).

Troubleshooting:
Initially, I misunderstood the pairing pattern and tried a recursive approach without memoization, which led to exponential time complexity. After realizing that this problem followed the Catalan recurrence, I switched to a bottom-up DP approach.

Another issue was handling odd values of n — these should return 0, since handshake pairs require even numbers of participants. Adding a simple check (if (n % 2 == 1) return 0;) fixed that edge case.

Reflection:
This problem was an excellent exercise in recognizing combinatorial patterns and converting them into a DP formulation. Understanding how each handshake divides the problem into smaller subproblems was key.

It reinforced my understanding of the Catalan sequence and how it appears in different combinatorial contexts like balanced parentheses, polygon triangulations, and handshake problems.

Complexity:

    Time: O(n²) — two nested loops for DP state computation.
    Space: O(n) — storing the DP table.




Weekly Reflection

This week’s focus was on strengthening my problem-solving and algorithmic reasoning through a mix of recursive, dynamic programming, and greedy problems from the TopCoder archives. Each problem challenged a different aspect of my thinking — from dependency resolution and combinatorial logic to optimization and handling edge cases.

Learning and Progress

I started the week with FillBox, which turned out to be a challenging 3D spatial reasoning problem. My initial recursive and volume-based attempts failed to properly manage cube placement and space partitioning. Although I wasn’t able to complete it fully, the process taught me how difficult true 3D decomposition can be, especially when overlap and space-fitting constraints come into play. Rather than getting stuck, I made the conscious choice to move on — a valuable reminder that stepping away from a tough problem to build confidence on another can be productive.

Moving to CorporationSalary was a turning point. The problem modeled a company’s hierarchy as a directed acyclic graph (DAG), where each employee’s salary depends on their subordinates. Implementing this with DFS and memoization helped me consolidate my understanding of recursion with caching. It also reinforced how base cases and dependency tracking are essential in recursive computations. By the end, I felt much more confident designing bottom-up recursive solutions.

Next, I tackled ProblemsToSolve, which required logical reasoning and careful state tracking. This problem deepened my understanding of how constraints (like parity in indexing) can complicate what seems like a straightforward iteration. Through several debugging cycles, I learned to maintain multiple simultaneous trackers (odd/even min and max values) — a key insight that ultimately led to a clean O(n) solution. It reminded me how iterative refinement and attention to small off-by-one or parity conditions can turn an almost-correct solution into a fully correct one.

The RockStar problem was a fun application of greedy algorithms. It involved sequencing songs based on tempo rules. My first attempts oversimplified the transitions, but through step-by-step testing, I identified edge cases — particularly when there were more slow-to-fast than fast-to-slow transitions. The final condition (if (sf > fs) return ans + 1;) captured those edge scenarios elegantly. It showed how small logical insights can make a greedy algorithm robust and reliable.

Finally, I completed HandsShaking, which introduced me to the Catalan number pattern — a classic in combinatorial mathematics. I started with recursion but soon realized that a bottom-up DP approach was necessary for efficiency. This problem tied together multiple themes I’d encountered throughout the week: recursion, DP, base cases, and recognizing deeper mathematical structures behind algorithmic patterns.

Key Takeaways

Recognizing Problem Structures:
Whether it’s a DAG (CorporationSalary), parity-based sequence (ProblemsToSolve), or Catalan sequence (HandsShaking), identifying underlying mathematical or structural patterns makes complex problems much simpler.

Balancing Recursion and DP:
Several problems (CorporationSalary, HandsShaking) reinforced when to transition from pure recursion to memoized or bottom-up DP to improve efficiency.

Handling Edge Cases Thoughtfully:
In RockStar and ProblemsToSolve, the difference between a partially correct and fully correct solution often came down to subtle edge conditions. Testing diverse inputs proved invaluable.

Learning from Failures:
FillBox reminded me that not every problem needs to be solved immediately — sometimes the real value lies in understanding why an approach fails and applying those lessons later.

Overall Reflection

This week’s work significantly improved my algorithmic reasoning, particularly around recursion, dynamic programming, and greedy logic. Each problem required a unique thought process, and moving through them sequentially helped me develop a stronger problem-solving intuition.

I also noticed improvement in debugging skills — instead of randomly trying fixes, I began methodically testing hypotheses and tracing small examples to pinpoint errors. By the end of the week, I felt more confident tackling both abstract combinatorial problems and concrete logic puzzles, balancing theoretical understanding with practical coding strategies.